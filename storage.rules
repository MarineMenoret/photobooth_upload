rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{userId}/{allPaths=**} {
      allow read, write: if request.auth.uid != null && ((request.auth.uid == users) || request.auth.uid == "user_admin");
    }
  }
}
